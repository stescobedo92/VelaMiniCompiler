# Run with: vela run examples/contracts.vela

include vela.core;

fn divide(total: Int, parts: Int) -> Int {
    assert parts != 0, "parts must not be zero";
    return total / parts;
}

fn main() -> Int {
    print(divide(42, 6));
    return 0;
}
