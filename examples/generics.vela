# Run with: vela run examples/generics.vela

include vela.core;

record Pair<T> {
    left: T;
    right: T;
}

fn first<T>(pair: Pair<T>) -> T {
    return pair.left;
}

fn main() -> Int {
    let pair = Pair<Int>(7, 11);
    let value: Int = first(pair);
    print(value);
    return 0;
}
